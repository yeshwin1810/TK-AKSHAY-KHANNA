int main()
{
  float side, area, temp;

  printf("Enter the Side of the square: ");
  scanf("%f",&side);

  temp =  4 ;
  area = temp * side  ;
  printf("Perimeter of square is: %.2f",area);

  return 0;
}
